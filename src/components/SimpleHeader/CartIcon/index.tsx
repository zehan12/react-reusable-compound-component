import { ShoppingCart } from "lucide-react";
import { FC } from "react";

const CartIcon:FC = () => {
    return <div>
        <ShoppingCart className="h-7 w-7" color="white" />
    </div>;
};

export default CartIcon;
